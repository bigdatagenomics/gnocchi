package net.fnothaft.gnocchi.primitives.genotype

import net.fnothaft.gnocchi.GnocchiFunSuite

class GenotypeStateSuite extends GnocchiFunSuite {

  // to Double tests
  ignore("GenotypeState.toDouble should exit gracefully when there are no valid calls.") {

  }

  ignore("GenotypeState.toDouble should count the number of matches to the alternate allele.") {

  }

  // to List tests
  ignore("GenotypeState.toList should split the genotype state into a list of genotype values: pipe delimiter.") {

  }

  ignore("GenotypeState.toList should split the genotype state into a list of genotype values: forward slash delimiter.") {

  }
}
